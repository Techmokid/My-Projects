void setup() {
  setupKM();
  delay(8000);
  startPlacing();
  
  //mineForwards(true); //Boolean tells it whether to crouch or not
  //moveForwardsBlocks(1);
  
  //placeRectangle(5,8);
  //printChatMsg("This is my Arduino Leonardo");
  //printChatMsg("Arduino Leonardo set to both mouse and keyboard control");
  //printChatMsg("User says: Don't worry, just so I can auto-crouch. Just a helpful little shortcut");
  //printChatMsg("End of user message. Proceeding with assigned task");
  
  //printChatMsg("[Arduino Leonardo] - Beginning automated task 1/1: Activate Crouch Walk");
  
  //stripMine(1,6);
}

void loop() { }
